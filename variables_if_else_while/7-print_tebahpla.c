#include <stdio.h>
#include <stdlib.h>
/**
 *  * main - Prints all the alphabet from z to a
 *   * Return: Always (Success)
 *    */

int main(void)
{
		char l;

			for (l = 'z'; l >= 'a'; l--)
					putchar(l);
				putchar('\n');

					return (0);
}
